<?xml version="1.0" encoding="utf-8"?>
<paths>
    <files-path name="downloads" path="downloads/" />
    <cache-path name="cache" path="." />
    <!-- FileLogger 내부 저장소 경로 (외부 저장소에서 이전, 보안 강화). -->
    <files-path name="log" path="LOG/" />
</paths>
